import React from 'react';

function Connect() {
    return (
        <section className="lets-connect" id="connect">
            <div className="thanks">
                <p style={{ fontSize: 'clamp(20px, 2vw, 40px)' }}>Thanks for wandering through this corner of the web.</p>
                <p style={{ fontSize: 'clamp(15px, 1.5vw, 40px)' }}>Sorry, I don’t do farewells.<br />Why part ways?</p>
                <p style={{ fontSize: 'clamp(20px, 3vw, 40px)' }}><a href="mailto:manushkalwari141@gmail.com">Lets Connect!</a></p>
            </div>
        </section>
    );
}

export default Connect;